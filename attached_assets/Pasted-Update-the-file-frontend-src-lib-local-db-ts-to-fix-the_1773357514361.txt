Update the file `frontend/src/lib/local-db.ts` to fix the build error and properly support the offline cache system.

Important:
Do NOT change the existing offline architecture or remove any existing tables.
Only extend the file safely.

Current problem:
`queryClient.ts` imports `cacheSet` and `cacheGet` from `local-db.ts`, but they are not properly implemented or typed.

Goal:
Add a cache store table and helper functions without breaking the existing IndexedDB schema.

Make the following changes.

---

1. Add a new interface above the database class:

```
export interface CacheItem {
  key: string;
  value: any;
  updatedAt: number;
}
```

---

2. Inside the `FinancialRadarDatabase` class add a typed table:

```
cache_store!: Table<CacheItem, string>;
```

The class should now include:

transactions
accounts
goals
debts
budgets
categories
xp_logs
streak_logs
offline_queue
cache_store

---

3. Update the Dexie schema version from:

```
this.version(2)
```

to

```
this.version(3)
```

---

4. Add the new table to the schema:

```
cache_store: "key, updatedAt"
```

Example final schema:

```
this.version(3).stores({
  transactions: "++id, remoteId, type, date, synced",
  accounts:     "++id, remoteId, type, synced",
  goals:        "++id, remoteId, synced",
  debts:        "++id, remoteId, synced",
  budgets:      "++id, remoteId, month, synced",
  categories:   "++id, remoteId, type, synced",
  xp_logs:      "++id, synced",
  streak_logs:  "++id, date, synced",
  offline_queue:"++id, endpoint, method, createdAt",
  cache_store:  "key, updatedAt"
});
```

---

5. After the database export add cache helper functions:

```
export async function cacheSet(key: string, value: any) {
  await localDb.cache_store.put({
    key,
    value,
    updatedAt: Date.now(),
  });
}

export async function cacheGet(key: string) {
  const item = await localDb.cache_store.get(key);
  return item?.value ?? null;
}
```

---

6. Do NOT modify the following tables or logic:

transactions
accounts
goals
debts
budgets
categories
xp_logs
streak_logs
offline_queue

These are part of the existing offline system and must remain unchanged.

---

Expected result:

* Vercel build succeeds
* `cacheSet` and `cacheGet` are properly exported
* IndexedDB schema migration works safely
* Offline queue system remains intact

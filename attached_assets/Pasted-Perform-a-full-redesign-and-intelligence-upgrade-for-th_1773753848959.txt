Perform a full redesign and intelligence upgrade for the web app focusing on:
- AI Advisor improvement
- Layout full-height fix
- Light mode color correction
- UI consistency across Market, Invest, Tools, AI pages

Reference:
Use Dashboard (Beranda) as the primary design reference (green gradient, soft contrast, readable UI).

----------------------------------------
1. 🎨 COLOR SYSTEM FIX (CRITICAL)
----------------------------------------

Problem:
Light mode is too bright and washed out.

Fix:

1. Replace pure white backgrounds:
   - from: bg-white
   - to: bg-[#F5F7F6] or bg-neutral-50

2. Card background:
   - use:
     bg-white
     border border-neutral-200
     shadow-sm

3. Text color:
   - Primary: text-neutral-800
   - Secondary: text-neutral-500
   - Avoid low-contrast text

4. Accent color:
   - Use green from dashboard:
     bg-green-600 / text-green-600

5. Dark mode (if used):
   - keep gradient like dashboard:
     from-green-900 to-green-800

----------------------------------------
2. 📐 FULL HEIGHT LAYOUT FIX (NO EMPTY SPACE)
----------------------------------------

Problem:
Pages have empty bottom space.

Fix:

1. Main container:
   - min-h-screen
   - flex flex-col

2. Content wrapper:
   - flex-grow

3. Structure:
   <Page>
     Header
     Content (flex-grow)
     Bottom spacing safe-area
   </Page>

4. Avoid:
   - fixed height containers
   - large margin-bottom

5. Use:
   - space-y-4 consistently

----------------------------------------
3. 🤖 AI ADVISOR UPGRADE (SMART + PERSONAL)
----------------------------------------

Problem:
AI still generic

Fix:

A. Inject user data into prompt:

- total_asset
- income
- expense
- saving_rate

B. Use structured response:

AI MUST return:
1. Insight (1 sentence)
2. Problem (if any)
3. Actionable advice (max 2 steps)

---

C. System Prompt:

"You are a smart personal finance advisor.
Always give:
- clear insight
- simple explanation
- actionable advice
Avoid generic answers."

---

D. Improve UI:

1. Chat container:
   - flex-grow
   - overflow-y-auto

2. Input fixed bottom:
   - sticky bottom-0

3. Remove empty space:
   - if no chat → show suggestions

---

E. Quick Templates (IMPORTANT):

- Analisis pengeluaranku
- Cara nabung lebih cepat
- Apakah saya boros?

Click → auto send

----------------------------------------
4. 📊 MARKET PAGE FIX
----------------------------------------

Fix layout:

1. Market snapshot:
   - grid grid-cols-2 gap-3

2. News:
   - compact list
   - max 3 items

3. Remove:
   - empty placeholder cards

----------------------------------------
5. 💰 INVEST PAGE FIX
----------------------------------------

1. Stock list:
   - use list style (like Rekening)

2. Portfolio:
   - compact card
   - show profit/loss clearly

3. Remove:
   - excessive empty space

----------------------------------------
6. 🛠️ TOOLS PAGE FIX
----------------------------------------

1. Convert tools into clickable cards:

- flex items-center justify-between
- icon + title + description

2. Add:
   - hover/active effect

3. Remove:
   - large empty bottom space

----------------------------------------
7. 🧩 CONSISTENCY RULE
----------------------------------------

All pages MUST use:

- same padding (px-4)
- same spacing (space-y-4)
- same card style
- same typography scale

DO NOT create new styles per page.

----------------------------------------
EXPECTED RESULT
----------------------------------------

- Light mode becomes readable & soft
- No empty space anywhere
- AI feels smart & personal
- All pages feel like one fintech app
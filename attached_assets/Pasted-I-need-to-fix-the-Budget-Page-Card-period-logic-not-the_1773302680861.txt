I need to fix the Budget Page Card period logic, not the dashboard and not the wizard UI.

The dashboard already works correctly and respects the user selected cycle.

The problem is only on the Budget Page Card where the PERIODE AKTIF still shows the calendar month (01 Mar – 31 Mar) even when the user selected Tanggal ke Tanggal.

Example from my screenshots:

Wizard confirmation correctly shows
12 Mar – 11 Apr

But after saving the plan the Budget Card shows
01 Mar – 31 Mar

So the Budget Card is ignoring the saved cycleType and cycleStartDay.

REQUIRED BEHAVIOR

The Budget Card must display the period based on the cycle type selected by the user.

1️⃣ If user selected Bulanan

Display:

01 Mar – last day of the month

The end date must be calculated dynamically:

const endOfMonth = new Date(year, month + 1, 0)

Examples:

01 Mar – 31 Mar
01 Apr – 30 Apr
01 Feb – 28 Feb

The subtitle under the date should show:

Bulanan
2️⃣ If user selected Tanggal ke Tanggal

Display the calculated cycle based on:

cycleStartDay
cycleType
planMonth

Example:

User selected start date = 12

Then the cycle must be:

12 Mar – 11 Apr

Not:

01 Mar – 31 Mar

The subtitle under the date should show:

Tanggal ke Tanggal
IMPORTANT

Do NOT change UI layout.

Only fix the logic that determines the period displayed on the Budget Page Card.

The card structure must stay exactly the same:

PERIODE AKTIF
[date range]

[subtitle]
WHERE TO FIX

Locate the code where the Budget Page card renders:

PERIODE AKTIF

This component is using a monthly fallback instead of reading the saved cycle.

Update it so it reads from:

budgetPlan.cycleType
budgetPlan.cycleStartDay

and computes the cycle correctly.

LOGIC SUMMARY

Use this logic:

if (cycleType === "monthly") {

 start = firstDayOfMonth
 end = lastDayOfMonth

 label = "Bulanan"

}

if (cycleType === "custom") {

 start = cycleStartDay of plan month
 end = start + 1 month - 1 day

 label = "Tanggal ke Tanggal"

}
EXPECTED RESULT

If wizard selected:

Tanggal mulai: 12 Mar

Budget Card must show:

PERIODE AKTIF
12 Mar – 11 Apr

Tanggal ke Tanggal

If wizard selected monthly:

PERIODE AKTIF
01 Mar – 31 Mar

Bulanan
DO NOT TOUCH

Do not modify:

dashboard calculations

wizard UI

allocation logic

budget categories

Only fix the period display logic in the Budget Page Card.
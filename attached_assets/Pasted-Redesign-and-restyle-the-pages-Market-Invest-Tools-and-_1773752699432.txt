Redesign and restyle the pages: Market, Invest, Tools, and AI Advisor to match the visual style and layout consistency of the existing pages: Dashboard (Beranda), Rekening, and Transaksi.

Design Goal:
- Clean fintech style
- Consistent spacing, typography, and card system
- No empty or dead space
- Feels like one unified app (not separate modules)

----------------------------------------
1. GLOBAL DESIGN SYSTEM (MANDATORY)
----------------------------------------

1. Use consistent container:
   - max-width layout
   - px-4 padding
   - vertical spacing: space-y-4

2. Card style (IMPORTANT):
   - rounded-2xl
   - border border-neutral-200/10
   - bg-white/5 or subtle gradient
   - shadow-sm
   - padding: p-4

3. Typography:
   - Title: text-lg font-semibold
   - Section label: text-xs uppercase opacity-60
   - Value: text-base font-medium
   - Avoid large empty headers

4. Spacing:
   - Use gap-3 or gap-4 consistently
   - Avoid large empty vertical gaps

----------------------------------------
2. MARKET PAGE REDESIGN
----------------------------------------

Structure:

A. Market Snapshot Card
- Show:
  Gold / USD / BTC
- Inline layout (like summary cards in dashboard)

B. Financial News Card
- Max 3 items
- Each item:
  title (line-clamp-2)
  small source + time
- No large empty container

C. Remove:
- empty placeholders
- oversized blank sections

----------------------------------------
3. INVEST PAGE REDESIGN
----------------------------------------

Fix layout:

1. Search bar:
   - inline with button
   - compact (not oversized)

2. Popular Stocks:
   - Convert to list style (like Rekening)
   - Each item:
     name
     price
     % change
     action (+)

3. Portfolio:
   - Same style as Rekening card
   - Show:
     stock
     qty
     avg price
     profit/loss

4. Remove:
   - large empty spacing
   - unnecessary section gaps

----------------------------------------
4. TOOLS PAGE REDESIGN
----------------------------------------

Current issue: too empty

Fix:

- Convert tools into interactive cards:

Each tool:
- icon (left)
- title
- short description
- clickable card (like menu)

Add:
- slight hover/press effect

Layout:
- space-y-3
- no large empty bottom space

----------------------------------------
5. AI ADVISOR REDESIGN
----------------------------------------

Current issue:
- terlalu kosong
- tidak engaging

Fix:

A. Top Summary Card:
- show:
  total asset
  income
  expense

B. Quick Action Chips:
- small rounded buttons:
  "Analisis pengeluaran"
  "Cara nabung lebih cepat"

C. Chat Area:
- compact input at bottom
- messages styled like chat bubble

D. Empty state:
- replace big empty space with:
  helpful suggestion text

----------------------------------------
6. REMOVE EMPTY SPACE (CRITICAL)
----------------------------------------

- Every section must have content
- Avoid fixed height containers
- Use:
  flex + flex-col + justify-between
- Let content define layout

----------------------------------------
7. CONSISTENCY RULE (VERY IMPORTANT)
----------------------------------------

All pages must follow:

- Same card radius
- Same padding
- Same text scale
- Same spacing rhythm

If a component exists in Dashboard:
→ reuse style (DO NOT reinvent)

----------------------------------------
EXPECTED RESULT
----------------------------------------

- All pages feel consistent
- No more "empty app feeling"
- Clean, modern fintech UI
- Smooth UX across all features